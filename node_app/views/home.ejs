<%- include('header.ejs') %>
    <% if (user) { %>
        <h1 class="text-center">Welcome, <%= user %>
        </h1>
        <% } else if (admin) { %>
            <h1 class="text-center">Welcome, <%= admin %>
            </h1>
            <% } else { %>
                <h1 class="text-center">Welcome, Guest</h1>
                <% } %>
                    <%- include('footer.ejs'); %>